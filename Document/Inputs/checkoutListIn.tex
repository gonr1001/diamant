\chapter{\eXit{} Conventions de codage}
Ce document est un complément de Java Code conventions
\section*{Checkout List pour une classe}



\begin{enumerate}
    \item bon nom du \verb!package!.
    \item code en accord avec convention Java.
    \item commentaire général bien fait et à la bonne place.
    \item commentaire général avec le nom de la classe.
    \item \verb!import!s, toujours avec le nom de la classe à importer (pas de \verb!*!).
    Ordonnées lignes comportant \verb!java!, ligne vide,
    lignes comportant \verb!javax.swing!, ligne vide,
    lignes comportant \verb!lib!, ligne vide lignes comportant les classes de l'application.
    \item commentaire de description de la responsabilité de la classe.
    \item données membres, toujours \verb!private!, sinon justifier. Un
    commentaire sur leur fonction.
    \item les données membres commencent toujours par un \verb!_!
    \item méthode avec commentaires sur paramètres et return, indiquer toutes les exceptions que la méthode
    peut jeter.
    \item une méthode doit aussi contenir des commentaires requires, modifies, effets. requires indique les
    contraintes possibles; modifies indique toutes les entrées
    modifiées; et  effets définit le comportement.
    \item méthode à redéfinir dans la plus part de classes \pro{toString}.
    \item class de tests de toutes les méthodes.
    \item \verb!set! et \verb!get! n'ont pas besoin de
    commentaires.
\end{enumerate}


\section{Cvs}
Il faut mettre l'option Preferences/Global Check out read only
sans coche, et ne pas utiliser Edit selection et Edit reserved
(les crayons dans la règle). Pour éviter le base!
